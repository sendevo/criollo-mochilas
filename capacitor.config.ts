import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.inta.criollom',
  appName: 'Criollo Mochilas',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
